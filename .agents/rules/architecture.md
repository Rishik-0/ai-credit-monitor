# Architecture Rules

This project MUST adhere to the following rules:
- **Manifest Version:** MUST use Manifest V3.
- **Framework & Build Tool:** MUST be scaffolded using React and Vite.
- **Styling:** MUST use Tailwind CSS for all styling.
- **State Management:** MUST use `chrome.storage.local` for all state and API key management.
